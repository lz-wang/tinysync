# TinySync

TinySync 把远端存储的文件同步到本地，并通过受控的公开 URL 把同步结果分享出去。本文件是领域术语表。

## 共享域

**共享（Share）**:
一条把本地文件、目录或同步任务本地根暴露为公开 URL 的显式策略；包含名称、启停与过期。
_Avoid_: 发布、Publish、PublishedFile、公开策略

**共享名称（Slug）**:
共享在公开 URL `/shared/` 之下的唯一标识段；由用户自定义或系统随机生成，二者同一字段、无独立展示名。
_Avoid_: public_path、公开路径、分享链接名

**共享目标（Target）**:
共享指向的本地路径：单文件、目录，或同步任务（Job）的本地根。创建时必须已存在于磁盘，创建后与 Job 生命周期解耦。
_Avoid_: local_path（API 字段名除外）

**浏览页**:
`/shared/<slug>`，唯一公开页面；展示共享目录的内容或被单独共享的文件。

**文件直链**:
`/shared/<slug>/<路径>` 指向具体文件的公开下载链接；指向目录或不存在内容的直链不可服务。

**同形 404**:
不可服务（禁用、过期、目标缺失、路径逃逸）的共享在直链侧与不存在的共享返回同样响应，不泄露存在性。

**可服务**:
共享处于启用且未过期的状态；是公开浏览和直链可用的共同前提。

## 运行域

**工作项（Work Item）**:
一轮运行计划处理的单个文件动作——下载、更新、跳过、释放或删除；是运行进度的计量单位，在计划完成后确定、不再增减。
_Avoid_: 文件数（与远端扫描到的文件数是不同概念）、任务

**运行进度（Run Progress）**:
一轮运行在执行过程中的瞬时完成情况：当前阶段、已完成工作项数与在途文件字节量。只存在于运行期间，进程内存态，不持久化。
_Avoid_: 实时统计（与最终统计 RunStats 是不同事实）

**已取消（Canceled）**:
用户手动停止导致的运行终态（run 级与文件级明细同用）；服务关闭、超时等非用户意愿的中断不属于取消，仍记失败。
_Avoid_: 中止、终止、aborted、interrupted

## 凭据域

**凭据（Credential）**:
一条命名的 secret 记录，独立于任何单个同步源存在，可被多个同步源同时引用；由用户显式创建、更新与删除。SSH 私钥形态下，私钥与其解密口令是不可分割的整体，随凭据存取。
_Avoid_: 密钥库、秘钥、钥匙串

**凭据引用（Credential Reference）**:
同步源配置中指向凭据的指针；一个源至多引用一条凭据。引用凭据的源不得再内联持有同类 secret——引用与内联互斥。
_Avoid_: 绑定、关联、外键

**内联凭据（Inline Credential）**:
直接存放在单个同步源自身之内的 secret（旧形态，继续可用）；与凭据引用并存但不混用。
_Avoid_: 私有凭据、本地凭据

**主机密钥指纹（Host Key Fingerprint）**:
SFTP 服务器主机公钥的 SHA256 标识，用于严格校验连接目标是否可信；属于同步源的连接配置。
_Avoid_: 指纹（单独使用时与公钥指纹混淆）

**公钥指纹（Public Key Fingerprint）**:
由凭据中的私钥派生的公钥 SHA256 标识，用于在人面前区分多把钥匙；随私钥内容唯一确定，不可手改，不参与任何校验决策。
_Avoid_: 指纹（单独使用时与主机密钥指纹混淆）、密钥 ID
