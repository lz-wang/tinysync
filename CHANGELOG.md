# 更新日志

本文件记录 TinySync 每次正式版本包含的用户可见变化，发布内容应与对应的
GitHub Release 摘要一致。

格式遵循 [Keep a Changelog 1.1.0](https://keepachangelog.com/zh-CN/1.1.0/)，
版本遵循[语义化版本 2.0.0](https://semver.org/lang/zh-CN/)。正式版本格式为
`x.y.z`；开发版本格式为 `dev-<commit日期>-<commit7>`。

开发期间向 `[Unreleased]` 写入；正式发布时将 `[Unreleased]` 改为
`[x.y.z] - yyyy-mm-dd`。Release workflow 只读取对应版本段落，
绝不根据 Git commit message 自动生成 Release Notes。

每个版本只使用以下分类：

- `新增`：面向用户的新特性。
- `修复`：面向用户的故障修复。
- `移除`：面向用户的特性或能力移除。

内部重构、测试、构建和文档维护不进入发布摘要，除非它们直接交付新特性或修复用户可见故障。

## [Unreleased]

### 新增

- 支持共享（Share）本地单文件、目录或同步任务的本地根目录：共享名称可选（即 URL slug，留空自动生成），支持过期时刻与启停，创建入口位于本地文件页的行内按钮与工具栏按钮。
- 新增仅凭 slug 访问的公开共享页 `/shared/<slug>`：支持目录下钻、分页、文件下载与复制链接，公开侧始终隐藏点文件；裸 `/shared` 重定向至主页，公开共享枚举 API 不提供。
- 新增无认证公开 API `GET /api/v1/public/shares/:slug/entries`（目录分页浏览）与共享文件直链 `/shared/<slug>/<file>`（支持 Range / HEAD），页面与直链均带 noindex。
- 新增 TinySync SVG 产品图标，浏览器标签页标题随页面、文件视图与运行详情自动更新。
- 文件菜单更名为「文件管理」，远端、本地与共享视图及当前浏览目录同步到浏览器地址，采用统一的固定高度文件管理器布局（位置栏、面包屑、分页表格、排序与下载），默认隐藏点文件且可切换显示。
- 同步源与同步任务页面改用全宽的可搜索、筛选、排序、分页与多选批量操作表格，关键删除操作增加确认，连接测试改为 toast 提示。
- Web UI 重构为可折叠侧边栏工作区，新增亮色、暗色与自动主题；用户设置支持头像与在线改密，API Token 管理移至独立设置入口。
- Web UI 移除各页面重复的标题与说明，页面、表单与提示统一提供中文文案。
- 创建或编辑同步源、同步任务时，表单面板固定为页面宽度的 60% 与高度的 80%，内容在面板内滚动。
- SFTP 同步源的主机密钥 SHA256 指纹改为可选项：填写时严格校验，留空时跳过校验。
- 同步源编辑面板将「启用」开关固定在底部操作栏，滚动表单时始终可见。
- 创建同步任务时可浏览并选择运行主机上的本地目录，不存在的路径可在创建前自动建立。
- 本地目录选择器固定尺寸并默认从运行用户 Home 目录开始，支持显示隐藏目录与在当前目录新建文件夹。
- 远端根目录选择器复用本地目录选择器的固定尺寸与操作方式，默认隐藏点开头条目并支持直接新建文件夹。
- 同步任务编辑面板按关联字段重排为双列布局，启用状态固定在底部操作栏。
- 同步源编辑面板按协议重排字段：WebDAV 与 SFTP 支持配置远端根目录（SFTP 留空使用登录用户 Home），S3 服务地址与密钥改为必填。
- 设置页改为带图标的顶部标签导航：帐号资料与安全设置归入「帐号设置」，标签切换同步浏览器地址。

### 修复

- 修复以 HTTP（非 localhost）访问 Web UI 时复制链接等剪贴板操作报错的问题：非安全上下文下自动回退到兼容写入方式。
- 修复 Web UI 的 Cron 计划显示独立时区输入的问题：缺省按运行机器的本地时区解释，与单次定时运行一致。
- 首次 `serve` 启动自动生成高熵管理员密码并仅在本机输出一次，密码变更立即废弃全部已有 Web Session。

### 移除

- 移除 v0.6 的「发布」功能与全部相关端点，由共享（`/api/v1/shares`、`/shared`）取代，不留兼容别名。
  升级注意：升级时 `published_files` 表被废弃且旧发布策略不迁移，升级后需重新创建共享。

## [0.9.0] - 2026-09-19

### 新增

- 新增数据目录单实例约束：同一数据目录的第二个 `serve` 实例启动立即失败并说明占用情况，进程异常退出后由操作系统自动释放锁。
- 新增数据库维护命令 `tinysync db check`（完整性报告）、`db backup`（一致性快照）与 `db restore`（校验后原子替换的离线恢复），三者与 `serve` 互斥执行，schema 较当前二进制新的备份拒绝恢复。
- 数据库迁移（含升级前）现在先执行完整性检查，损坏的数据库拒绝继续迁移；优雅关闭顺序收口后，干净退出不再遗留膨胀的 WAL 文件。
- 传输重试只针对瞬时错误，认证失败、404 等 4xx、权限不足、磁盘耗尽等确定性失败不再重试；新增 `--transfer-timeout` 配置单文件单次传输超时（默认关闭），超时尝试会重试且不终结整轮运行。
- 进程异常退出遗留的下载临时文件在启动时自动安全清理：只删除与内部临时文件名形态严格匹配的普通文件，用户文件一律不动。
- 新增本地映射 preflight：Windows 保留名、非法字符、大小写冲突等兼容性问题在任何本地变更前使整轮同步失败。
- 新增请求与同步链路日志关联：每个 HTTP 请求回写 `X-Request-ID` 并输出 access log，每轮同步输出启动与终态事件，进程硬崩溃后日志仍留有启动痕迹。

## [0.8.0] - 2026-09-18

### 新增

- 新增 MCP Streamable HTTP 端点 `POST /mcp`：Agent / LLM 可查询与执行 TinySync，完全复用现有 API Token 认证与 `read` / `run` / `admin` scope。
- 新增 7 个 MCP tools：`list_sources`、`list_jobs`、`get_job`、`run_sync`、`get_sync_run`、`search_files` 与 `get_file_info`，不提供任何配置修改类 tool。
- 新增本地同步文件搜索：以 Job 为命名空间从 `managed_files` 检索已同步文件（大小写不敏感子串匹配，默认 50 条、上限 200 条），本地已删除的记录不返回。
- 新增小型文本 MCP resource `tinysync://jobs/{job_id}/files/{path}`，仅承载 ≤ 256 KiB 的 UTF-8 普通文件。
- 大文件不经 MCP 搬运：`get_file_info` 返回 `download_url`，客户端携带同一 Bearer token 走现有下载端点，不产生匿名临时链接。

### 修复

- 修复 MCP `list_sources` 与 `list_jobs` 在超大 offset 下的整数溢出（现稳定返回空页），并修正工具的幂等性标注。
- 修复 `search_files` 将本地权限、I/O 等访问失败误报为「没有匹配文件」的问题：现在只忽略已不存在的 managed 文件。
- 修复未注册的 `/mcp/*` 路径被 Web SPA fallback 返回 HTML 的问题，现统一返回 404。

## [0.7.0] - 2026-09-18

### 新增

- 新增管理员认证基线：`tinysync auth set-password` 初始化或重置管理员密码（重置立即废弃全部已有 Web Session），密码未初始化时 `serve` 拒绝启动。
- REST API 引入认证：除少数公开端点外，所有 `/api/v1` 端点默认拒绝匿名访问（401），Web 端通过登录建立 HttpOnly Session Cookie，新增会话查询与登出接口。
- 新增 API Token：支持 `read` / `run` / `admin` 三种 scope、可选过期时刻与幂等撤销，raw token 仅创建响应返回一次。
- Web 界面新增登录页与 API Tokens 管理页，会话过期自动跳转登录页。

## [0.6.0] - 2026-09-17

### 新增

- 支持远端与本地文件浏览：Remote 视图分页浏览三种协议的远端目录，Local 视图以 Job 为入口浏览同步根并区分 managed / unmanaged 文件，大目录采用虚拟列表渲染。
- 支持文件下载：远端文件流式下载，本地文件下载支持 Range 断点请求与 HEAD。
- 支持 HTTP 发布策略：把同步后的 managed 本地文件显式发布为 `/published/...` 公开 URL，支持启停、过期时刻与路径冲突保护，禁用或过期后统一返回 404。
- Web 界面新增 Files 页面：Remote / Local / Published 三个视图，支持目录导航、下载、一键同步与远端目录选择器。

### 修复

- 修复本地文件浏览的元信息查询可经父目录 symlink 越出同步根目录、泄露根外文件元信息的问题。
- 修复发布文件被替换为 symlink 后公开 URL 可读取同步根之外文件的问题：服务以创建时固化的 canonical 路径为身份，组件变成 symlink 即返回 404。
- 修复远端下载 0 字节文件缺少 `Content-Length: 0` 响应头的问题。
- 发布策略的创建与更新接口改用严格 JSON 解码，未知字段与尾随数据一律返回 400。

## [0.5.0] - 2026-09-17

### 新增

- Source 支持多协议：在 WebDAV 之外新增 S3（自建 endpoint、path-style、prefix）与 SFTP（password / private key 认证、host key SHA256 严格校验）只读 Source，共用同一同步引擎、调度与运行历史。
- Source 配置拆分为非敏感 `config` 与 secret `credentials`：secret 永不回显，被同步任务引用的 Source 的 remote identity 受完整保护。
- 跨协议统一 logical path 校验：包含反斜杠、dot segments、重复分隔符或 NUL 的远端路径在协议边界整体失败，不再进入本地文件映射。
- Web 界面支持创建 / 编辑三种协议的 Source：动态表单、编辑时类型只读、secret 三态（保留 / 替换 / 清除）。
- v0.4 数据库升级时自动完成 schema 迁移并先备份数据库，存量 WebDAV Source 升级后同步行为不变。

### 修复

- 修复优雅关闭无法立即中断 SFTP 等有连接协议传输的问题：远端连接生命周期统一挂在运行取消链上。
- 远端连接建立失败现在记录为一轮失败的运行历史，而不是在触发运行的接口上同步报错。
- 修复连接测试把连接建立阶段失败误报为 500 的问题，恢复返回 200 与 `ok=false` 的既有契约。

## [0.4.0] - 2026-09-16

### 新增

- 支持 Job 自动调度：once（指定时刻，离线错过补执行一次）、interval（持久化锚点不漂移）与 cron（标准 5-field 表达式 + IANA 时区）；运行重叠时调度自动跳过并记录原因。
- 同步运行历史持久化：每轮运行及文件级变更明细落库，重启后仍可查询；异常退出遗留的运行自动收敛为失败，每 Job 保留最近 500 轮。
- 提供同步历史 REST API（`GET /api/v1/runs` 系列），`/jobs/:id/status` 改读持久化历史并附带 `next_run_at`，Job 配置支持 schedule。
- Web 界面新增 History 页面（运行列表与文件变化时间线），Jobs 页面支持调度配置与展示。
- 新增 `--max-concurrent-jobs` / `--max-concurrent-transfers` 配置并发 Job 数（默认 1）与远端文件并行下载上限（默认 4）。

### 修复

- 修正 `--max-concurrent-transfers` 语义：下载上限约束整个进程所有 Job 的总和，不再按 Job 叠加。
- 修复配置变更与运行启动的竞态：修改 / 删除与启动经协调位原子互斥，杜绝旧配置的运行写入新配置的元数据。
- 任一文件传输失败后立即取消其余在途下载，失败后不再改动本地文件。
- 被跳过的调度运行同样纳入每 Job 500 轮历史容量回收，不再无限堆积。
- 修复调度器内部读取故障吞掉到期触发的问题：恢复后补上本轮触发且不重复执行。
- Job 的 schedule 接口按 discriminated union 严格校验，非法字段组合与未知类型返回 400。
- 对 interval 调度发送同值更新不再重置触发相位。
- `GET /api/v1/runs/:id/items` 对不存在的 run 返回 404，与 `runs/:id` 语义一致。
- 修复 Web 编辑器丢失 once 调度秒精度的问题：未改动保存不再产生调度更新。
- 修复 once 调度与运行历史清理的冲突：消费状态改存于 Job 自身持久化字段，历史裁剪后不会重复触发。
- 调度触发在单次内部故障下不再丢失：本轮不推进游标，恢复后自动补上且不重复执行。
- 优雅关闭时运行终态可靠落库，关闭期间新的运行请求以 503 拒绝。
- 修复启动登记失败后的内部资源泄漏（此前导致之后的优雅关闭无法及时完成）。
- once 调度的执行与消费判定原子落库，消除同一 once 被反复触发的窗口。
- 配置变更期间到期的调度触发不再被永久跳过，稍后重试并正常执行。

## [0.3.0] - 2026-09-16

### 新增

- 支持 WebDAV 单向同步 Job：手动触发 Copy / Mirror 同步，include / exclude 过滤，原子下载（临时文件 + 替换），Mirror 只删除远端已消失且归本 Job 管理的本地文件。
- 提供 Sync Job 的 REST API：创建、查询、更新、删除及手动运行与状态查询。
- Web 界面新增 Jobs 管理页面，支持配置同步参数、一键手动运行与查看进度、统计及失败原因。
- 被 Sync Job 引用的 Source 以 `409` 拒绝删除。

### 修复

- 修复传输中断后文件可能被误跳过、本地长期停留旧版本的问题：中断的传输必定在后续运行中重试直至收敛。
- 远端无法提供可比对指纹时按已变更处理并重新传输，同尺寸的内容变更不再被漏掉。
- Mirror 删除前校验本地路径组件，父目录为 symlink 时拒绝删除，杜绝越界删除。
- 修复本地目录重叠检查在根路径（如 `/`）下漏判的问题。
- 修复非根 RemoteRoot 之下远端返回 Source 根路径时可能越界扫描的问题。
- 同步运行期间以 `409` 拒绝该 Job 的修改与删除，Web 界面同步禁用对应按钮。
- 创建 / 更新 Job 时即时校验 include / exclude pattern，非法 pattern 直接拒绝。
- 被 Sync Job 引用的 Source 以 `409` 拒绝修改 endpoint，防止更换远端后 Mirror 误删本地文件。

## [0.2.0] - 2026-09-15

### 新增

- 支持持久化管理 WebDAV Source。
- 提供 WebDAV Source 的 REST API 与 Web 管理界面。
- 支持验证 WebDAV Source 连接状态。

## [0.1.0] - 2026-09-14

### 新增

- 提供 TinySync 初始可运行服务骨架。
- 提供内嵌 Web UI。
- 提供服务健康状态与版本查询 API。
- 支持 Linux、macOS、Windows 的 amd64/arm64 构建。
